# microservice specific routes

GET         /lookup/:sicCode                         @controllers.LookupController.lookup(sicCode)
GET         /search                                  @controllers.SearchController.search(query, pageResults: Option[Int], page: Option[Int], sector: Option[String])
